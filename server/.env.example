# Copy to .env for local development (do not commit .env).
# On Render: set the same keys in the service Environment (Blueprint uses sync: false for these).

# Deployed PrioritAI ML (Cloud Run) — no trailing slash.
ML_SERVICE_URL=https://prioritai-ml-973722445460.europe-west1.run.app

# Optional: only if ML_SERVICE_API_KEY is set on the Cloud Run service.
# ML_SERVICE_API_KEY=
