# ─────────────────────────────────────────────────────────────────────────────
# PrioritAI — Environment Variables Template
# Copy this file to `.env` and fill in the values before running the server.
# The `.env` file is loaded automatically by python-dotenv when the app starts.
# ─────────────────────────────────────────────────────────────────────────────

# ── Application ───────────────────────────────────────────────────────────────
# Environment mode: "development" | "production"
APP_ENV=development

# Log level for the FastAPI / Uvicorn server.
LOG_LEVEL=info

# ── Database ───────────────────────────────────────────────────────────────────
# PostgreSQL connection string. For local dev with Docker: postgresql://prioritai:prioritai@localhost:5432/prioritai
DATABASE_URL=postgresql://prioritai:prioritai@localhost:5432/prioritai

# ── Frontend (Vite build-time variable) ───────────────────────────────────────
# URL the browser uses to reach the FastAPI backend.
# Docker Compose sets this to /api (Nginx reverse-proxy) at build time.
# For local `npm run dev`, the default in api.ts (http://localhost:8000) is used.
VITE_API_URL=http://localhost:8000

# ── Groq LLM ───────────────────────────────────────────────────────────────────
# Get your free API key at: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here